# Environment Variables for N8N Deployment
# Set these in Render Dashboard → Your Service → Environment

# REQUIRED VARIABLES
N8N_BASIC_AUTH_PASSWORD=choose-your-secure-password-here
N8N_ENCRYPTION_KEY=8709edc36787eefdc672f5964dfe9554a651dc4f4cf67cd631e6c9165d8d5900

# OPTIONAL: AI Service API Keys (only if using AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here
GOOGLE_AI_API_KEY=your-google-gemini-api-key-here

# OPTIONAL: External Service Credentials (only if needed)
GMAIL_CLIENT_ID=your-gmail-oauth-client-id
GMAIL_CLIENT_SECRET=your-gmail-oauth-client-secret
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# INSTRUCTIONS:
# 1. Copy the N8N_ENCRYPTION_KEY exactly as shown above
# 2. Choose a strong password for N8N_BASIC_AUTH_PASSWORD  
# 3. Add these in: Render Dashboard → Services → Your Service → Environment
# 4. AI and external service keys are optional - add only when needed